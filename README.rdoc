= Introduction

basic syn packet generator

Required args:
        -c count of packets to send
        -t real target ip4 address (1.2.3.4)
        -s spoofed source ip4 address (1.2.3.4)
        -p target port
